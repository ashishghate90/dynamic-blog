<div class="card mb-3" id="comment_<%= comment.id %>">
  <div class="card-body">
    <div class="d-flex justify-content-between align-items-start">
      <div class="flex-grow-1">
        <h6 class="card-subtitle mb-2 text-muted">
          <%= comment.user.display_name %> • <%= time_ago_in_words(comment.created_at) %> ago
        </h6>
        <p class="card-text"><%= simple_format(comment.content) %></p>
      </div>
      <!-- No action buttons in broadcast version to avoid Devise issues -->
    </div>
  </div>
</div>